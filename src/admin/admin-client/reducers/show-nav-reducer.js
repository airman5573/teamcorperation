export function showPointNavReducer(state = null, action) {
  switch(action.type) {
    default:
      return state;
  }
}

export function showPuzzleNavReducer(state = null, action) {
  switch(action.type) {
    default:
      return state;
  }
}